The code files are stored right here in your workspace directory

c:\Users\CC\WorkBuddy\20260402153118\ 

Here's what's in there

File	Purpose
index.html	Main HTML page — the WeStay platform UI structure
styles.css	All styling — sidebar, cards, tables, charts, chatbot
app.js	Interactive logic — navigation, charts, chatbot, data
server.js	Simple Node.js HTTP server to serve the prototype locally

You can open these files directly in any code editor. If you want to view the live prototype, run
node server.js

from that directory, then visit  http://localhost:3456 in your browser.